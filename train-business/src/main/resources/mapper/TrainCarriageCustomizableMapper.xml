<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjl.train.business.mapper.TrainCarriageCustomizableMapper">

    <select id="selectByUnique" resultType="com.zjl.train.business.entity.TrainCarriage">
        SELECT *
        FROM train_carriage
        WHERE name = #{name}
    </select>
</mapper>